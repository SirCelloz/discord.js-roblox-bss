const glitters = require('../boosts/glitters')
const superSmoothies = require('../boosts/superSmoothies')
const softWax = require('../wax/softWax')
const swirled = require('../wax/swirled')

const PetalPlanter = {
  magicBeans: 100,
  glitters: 100,
  softWaxes: 250,
  swirled: 50,
  superSmoothies: 25,

  totalGlitter : () => {
    
  }
};

module.exports = PetalPlanter;
